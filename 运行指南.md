# 🎮 RaCFormer 运行指南 - 小白也能懂的版本

> 这份指南就像"游戏攻略"，一步一步教你怎么跑起来这个模型！
> 不管你懂不懂代码，跟着做就行～

---

## 📚 目录

- [第0步：准备工作（必看！）](#第0步准备工作必看)
- [第1步：安装&#34;工具箱&#34;](#第1步安装工具箱)
- [第2步：准备数据](#第2步准备数据)
- [第3步：开始训练](#第3步开始训练)
- [第4步：测试模型](#第4步测试模型)
- [第5步：看结果](#第5步看结果)
- [常见问题解决](#常见问题解决)

---

## 第0步：准备工作（必看！）

### 📋 需要什么？

就像玩游戏要准备手柄、电脑一样，你需要准备：

1. **一台电脑**：

   - 有NVIDIA显卡（推荐：RTX 3090 或更好的）
   - 显存至少 **24GB**（如果只有12GB也能跑，但要调参数）
   - 硬盘空间 **至少500GB**（数据集很大）
2. **操作系统**：

   - Linux（推荐 Ubuntu 20.04 或 22.04）
   - Windows也可以（但需要WSL2）
3. **时间**：

   - 第一次安装：约 **1-2小时**
   - 训练一个模型：约 **2-3天**（看显卡性能）

### ✅ 检查清单

在开始之前，用这个清单检查一下：

- [ ] 显卡驱动已安装（运行 `nvidia-smi` 能看到显卡信息）
- [ ] 有 `conda` 或 `miniconda`（用来管理Python环境）
- [ ] 网络能连外网（要下载一些包）
- [ ] 心情不错，准备学习新东西！✨

---

## 第1步：安装"工具箱"

就像玩游戏前要安装游戏客户端，我们要装一些"工具"。

### 1.1 创建Python环境

想象成给这个项目准备一个"独立的小房间"，不会和其他项目冲突。

```bash
# 打开终端（Terminal），输入以下命令：

# 1. 创建一个叫"racformer"的环境，用Python 3.8
conda create -n racformer python=3.8 -y

# 2. 进入这个环境（以后每次运行都要先进入）
conda activate racformer

# 3. 检查是否成功（应该显示Python 3.8.xx）
python --version
```

**💡 小贴士**：

- 如果看到 `(racformer)` 出现在命令行前面，说明成功了！
- 以后每次打开新终端，都要先运行 `conda activate racformer`

### 1.2 安装PyTorch（深度学习框架）

PyTorch就像是"游戏引擎"，是整个项目的基础。

```bash
# 安装PyTorch（GPU版本，支持CUDA 11.3）
pip install torch==1.12.0+cu113 torchvision==0.13.0+cu113 --extra-index-url https://download.pytorch.org/whl/cu113

# 检查是否安装成功（应该显示True）
python -c "import torch; print(torch.cuda.is_available())"
```

**⚠️ 注意**：

- 如果显示 `False`，说明PyTorch没找到显卡，需要检查CUDA版本
- 如果你的显卡驱动支持CUDA 11.7，可以把 `cu113`改成 `cu117`

### 1.3 安装MMDetection3D及依赖

这是RaCFormer的"核心框架"。

```bash
# 先安装一些基础工具
pip install openmim
mim install mmcv-full==1.6.0
mim install mmdet==2.28.2
mim install mmsegmentation==0.30.0

# 进入项目文件夹
cd RaCFormer

# 安装MMDetection3D（编译模式）
pip install -v -e .

# 安装其他必要的包
pip install -r requirements.txt
```

**💡 这一步可能比较慢，耐心等待...**

### 1.4 安装额外的工具

```bash
# 安装图像处理加速库
pip install pillow-simd
pip install pyturbojpeg

# 安装timm库（用于RadarBEVNet）
pip install timm==0.9.2
```

### 1.5 验证安装

检查所有东西是不是都装好了：

```bash
# 运行这个命令检查
python -c "
import torch
import mmdet
import mmdet3d
import mmcv
print('PyTorch版本:', torch.__version__)
print('MMCV版本:', mmcv.__version__)
print('MMDet版本:', mmdet.__version__)
print('MMDet3D版本:', mmdet3d.__version__)
print('CUDA可用:', torch.cuda.is_available())
print('显卡数量:', torch.cuda.device_count())
print('✅ 所有工具安装成功！')
"
```

如果没报错，恭喜你，第一步完成啦！🎉

---

## 第2步：准备数据

就像玩游戏需要下载地图一样，我们需要准备数据集。

### 2.1 下载nuScenes数据集

这个数据集包含了很多自动驾驶场景的录像和标注。

**方法1：官方下载（推荐）**

1. 去 [nuScenes官网](https://www.nuscenes.org/nuscenes) 注册账号
2. 下载以下文件（一共约**350GB**，记得准备好硬盘）：

   - Full dataset (v1.0)
     - `v1.0-trainval01_blobs.tgz` ~ `v1.0-trainval10_blobs.tgz`（训练集）
     - `v1.0-test_blobs.tgz`（测试集，可选）
   - Metadata
     - `v1.0-trainval_meta.tgz`
     - `v1.0-test_meta.tgz`（可选）
3. 解压到 `data/nuscenes/` 文件夹

**方法2：使用已有数据（如果你的团队有）**

如果你的实验室或团队已经下载过，直接复制过来就行：

```bash
# 假设数据在 /path/to/nuscenes
ln -s /path/to/nuscenes data/nuscenes
```

### 2.2 整理数据结构

下载完后，文件夹应该长这样：

```
data/nuscenes/
├── maps/              # 地图数据
├── samples/           # 关键帧数据（图片、雷达点云）
│   ├── CAM_FRONT/
│   ├── CAM_FRONT_LEFT/
│   ├── CAM_FRONT_RIGHT/
│   ├── CAM_BACK/
│   ├── CAM_BACK_LEFT/
│   ├── CAM_BACK_RIGHT/
│   └── RADAR_FRONT/
│   └── RADAR_FRONT_LEFT/
│   └── RADAR_FRONT_RIGHT/
│   └── RADAR_BACK_LEFT/
│   └── RADAR_BACK_RIGHT/
├── sweeps/            # 中间帧数据
└── v1.0-trainval/     # 标注文件（JSON）
```

### 2.3 生成数据信息文件

这一步是把原始数据"翻译"成模型能读懂的格式。

```bash
# 进入RaCFormer文件夹
cd RaCFormer

# 运行数据预处理脚本
python tools/create_data.py nuscenes --root-path ./data/nuscenes --out-dir ./data/nuscenes --extra-tag nuscenes
```

**⏱️ 这一步要10-30分钟，泡杯茶等一下～**

完成后，应该生成这些文件：

```
data/nuscenes/
├── nuscenes_infos_train_sweep.pkl    # 训练集信息
├── nuscenes_infos_val_sweep.pkl      # 验证集信息
└── nuscenes_infos_test_sweep.pkl     # 测试集信息（可选）
```

### 2.4 下载预训练权重（可选但推荐）

就像玩游戏可以"继承存档"，用预训练权重能加快训练速度。

```bash
# 创建预训练模型文件夹
mkdir -p pretrain

# 下载预训练权重（约500MB）
# 方法1：用wget
wget https://download.openmmlab.com/mmdetection/v2.0/cascade_rcnn/cascade_mask_rcnn_r50_fpn_20e_nuim/cascade_mask_rcnn_r50_fpn_coco-20e_20e_nuim_20201009_124951-40963960.pth -O pretrain/cascade_mask_rcnn_r50_fpn_coco-20e_20e_nuim_20201009_124951-40963960.pth

# 方法2：如果网络不好，可以从浏览器下载后放到pretrain/文件夹
```

---

## 第3步：开始训练

现在万事俱备，可以开始"玩游戏"（训练模型）了！

### 3.1 单卡训练（1张显卡）

如果你只有1张显卡：

```bash
# 进入RaCFormer文件夹
cd RaCFormer

# 激活环境（如果还没激活）
conda activate racformer

# 🚀 开始训练！
python tools/train.py configs/racformer_with_rhgm_radarbevnet.py
```

**💡 小贴士**：

- 看到一堆输出信息是正常的，不要慌！
- 每隔50次迭代会打印一次进度
- 训练会自动保存checkpoint（检查点）

### 3.2 多卡训练（多张显卡，更快）

如果你有多张显卡（比如2张3090）：

```bash
# 用2张显卡训练（把2改成你的显卡数量）
bash tools/dist_train.sh configs/racformer_with_rhgm_radarbevnet.py 2
```

### 3.3 后台运行（推荐）

因为训练要好几天，建议用 `nohup`或 `tmux`让它在后台跑：

**方法1：用nohup**

```bash
# 后台运行，日志保存到train.log
nohup python tools/train.py configs/racformer_with_rhgm_radarbevnet.py > train.log 2>&1 &

# 查看日志
tail -f train.log
```

**方法2：用tmux（更推荐）**

```bash
# 创建一个名为"training"的会话
tmux new -s training

# 在tmux里开始训练
python tools/train.py configs/racformer_with_rhgm_radarbevnet.py

# 按 Ctrl+B，然后按 D，可以"挂起"会话（训练继续跑）

# 以后想看进度，重新连接：
tmux attach -t training
```

### 3.4 监控训练过程

训练开始后，你可以：

**1. 看终端输出**

```
[2025-12-23 10:30:15,123] Epoch [1][50/1234] lr: 0.000133, loss: 2.345, time: 0.5s
[2025-12-23 10:30:25,456] Epoch [1][100/1234] lr: 0.000267, loss: 2.123, time: 0.5s
```

- `Epoch [1]`：第1轮
- `[50/1234]`：当前进度（第50步，共1234步）
- `loss: 2.345`：损失值（越小越好，通常从3-5降到1以下）

**2. 用TensorBoard可视化**

```bash
# 在另一个终端运行
tensorboard --logdir=work_dirs/racformer_with_rhgm_radarbevnet --port=6006

# 然后在浏览器打开 http://localhost:6006
```

可以看到漂亮的曲线图！📈

### 3.5 中断和恢复训练

训练中途出问题了？没关系，可以恢复：

```bash
# 找到最新的checkpoint（在work_dirs/racformer_with_rhgm_radarbevnet/）
ls -lt work_dirs/racformer_with_rhgm_radarbevnet/*.pth

# 修改配置文件，把resume_from改成checkpoint路径
# 或者直接在命令行指定：
python tools/train.py configs/racformer_with_rhgm_radarbevnet.py --resume-from work_dirs/racformer_with_rhgm_radarbevnet/epoch_10.pth
```

---

## 第4步：测试模型

训练完成（或者想测试别人训练好的模型）：

### 4.1 单张图片测试

```bash
# 用训练好的模型测试
python tools/test.py \
    configs/racformer_with_rhgm_radarbevnet.py \
    work_dirs/racformer_with_rhgm_radarbevnet/epoch_36.pth \
    --eval bbox
```

### 4.2 在验证集上评估

```bash
# 在整个验证集上测试，得到mAP等指标
python tools/test.py \
    configs/racformer_with_rhgm_radarbevnet.py \
    work_dirs/racformer_with_rhgm_radarbevnet/epoch_36.pth \
    --eval bbox \
    --eval-options 'jsonfile_prefix=./results'
```

### 4.3 可视化结果

```bash
# 生成可视化图片（会保存到results/文件夹）
python tools/test.py \
    configs/racformer_with_rhgm_radarbevnet.py \
    work_dirs/racformer_with_rhgm_radarbevnet/epoch_36.pth \
    --eval bbox \
    --show-dir results/visualizations
```

然后可以在 `results/visualizations/` 文件夹里看到带检测框的图片！

---

## 第5步：看结果

### 5.1 理解评估指标

测试完会显示一堆数字，主要看这几个：

```
+----------+--------+--------+--------+
| Class    | AP     | ATE    | ASE    |
+----------+--------+--------+--------+
| car      | 0.850  | 0.35   | 0.25   |
| truck    | 0.720  | 0.45   | 0.30   |
| ...      | ...    | ...    | ...    |
+----------+--------+--------+--------+
| mAP      | 0.650  | -      | -      |
| NDS      | 0.710  | -      | -      |
+----------+--------+--------+--------+
```

**各个指标的意思**：

- **mAP**（越大越好）：检测准确率，满分1.0

  - 0.65 = 很不错 ✅
  - 0.50-0.65 = 还可以 😊
  - <0.50 = 需要改进 😢
- **NDS**（越大越好）：nuScenes官方综合评分

  - > 0.70 = 顶级水平 🏆
    >
  - 0.60-0.70 = 很好 ✅
  - <0.60 = 需要优化
- **ATE**（越小越好）：位置误差（单位：米）
- **ASE**（越小越好）：尺寸误差

### 5.2 对比原始RaCFormer

你可以对比一下加了RHGM和RadarBEVNet后，效果有没有提升：

| 模型              | mAP   | NDS   |
| ----------------- | ----- | ----- |
| 原始RaCFormer     | 0.645 | 0.695 |
| +RHGM+RadarBEVNet | 0.65x | 0.71x |

**期望提升**：mAP和NDS应该各提升1-3个百分点

---

## 常见问题解决

### 问题1：显存不够（CUDA out of memory）

**症状**：

```
RuntimeError: CUDA out of memory. Tried to allocate 2.50 GiB
```

**解决方案**：

```bash
# 方法1：减小batch_size
# 打开配置文件 configs/racformer_with_rhgm_radarbevnet.py
# 把 batch_size = 2 改成 batch_size = 1

# 方法2：减少虚拟点数量
# 把 num_virtual_points=100 改成 num_virtual_points=50

# 方法3：减少历史帧数
# 把 num_frames = 8 改成 num_frames = 4
```

### 问题2：找不到数据文件（FileNotFoundError）

**症状**：

```
FileNotFoundError: data/nuscenes/nuscenes_infos_train_sweep.pkl not found
```

**解决方案**：

```bash
# 重新运行数据预处理
cd RaCFormer
python tools/create_data.py nuscenes --root-path ./data/nuscenes --out-dir ./data/nuscenes --extra-tag nuscenes
```

### 问题3：训练速度很慢

**症状**：每个iteration要好几秒

**解决方案**：

```bash
# 1. 检查是否在用GPU
python -c "import torch; print(torch.cuda.is_available())"  # 应该是True

# 2. 增加数据加载线程
# 打开配置文件，把 workers_per_gpu=4 改成 workers_per_gpu=8

# 3. 安装加速库
pip install pillow-simd pyturbojpeg
```

### 问题4：导入模块失败（ModuleNotFoundError）

**症状**：

```
ModuleNotFoundError: No module named 'mmcv'
```

**解决方案**：

```bash
# 1. 确认环境激活了
conda activate racformer

# 2. 重新安装依赖
pip install openmim
mim install mmcv-full==1.6.0
mim install mmdet==2.28.2
```

### 问题5：Loss变成NaN（训练崩溃）

**症状**：

```
[2025-12-23 10:30:15] loss: nan
```

**解决方案**：

```bash
# 方法1：降低学习率
# 配置文件里把 lr=4e-4 改成 lr=2e-4

# 方法2：检查数据是否正常
# 看看是不是某些数据有问题

# 方法3：从checkpoint重新开始
python tools/train.py configs/racformer_with_rhgm_radarbevnet.py --resume-from work_dirs/.../epoch_X.pth
```

### 问题6：多卡训练报错

**症状**：

```
RuntimeError: Address already in use
```

**解决方案**：

```bash
# 方法1：指定不同的端口
MASTER_PORT=29501 bash tools/dist_train.sh configs/racformer_with_rhgm_radarbevnet.py 2

# 方法2：杀掉之前的进程
ps aux | grep python
kill -9 <进程ID>
```

### 问题7：RHGM模块不生效

**症状**：训练时日志里没看到RHGM相关输出

**检查方法**：

```bash
# 检查配置文件里是否启用了RHGM
grep "enabled=True" configs/racformer_with_rhgm_radarbevnet.py

# 检查模块是否正确注册
python -c "from RaCFormer.models import RHGM; print('RHGM模块导入成功')"
```

---

## 🎓 进阶技巧

### 技巧1：调参攻略

**如果想要更好的效果，可以调整这些参数**：

| 参数               | 位置                 | 默认值    | 调整建议                          |
| ------------------ | -------------------- | --------- | --------------------------------- |
| num_virtual_points | rhgm_module          | 100       | 增加到150-200（提升细节，但变慢） |
| gauss_sigma        | rhgm_module          | 7         | 调小（5）让虚拟点更集中           |
| feat_channels      | radar_bev_net_module | [64, 128] | 加深成[64, 128, 256]（更强特征）  |
| lr                 | optimizer            | 4e-4      | 根据batch_size调整                |
| total_epochs       | 训练参数             | 36        | 增加到48或60（充分训练）          |

### 技巧2：快速实验

**想快速验证想法？**

```bash
# 用少量数据训练（toy experiment）
# 修改配置文件：
data = dict(
    train=dict(
        ann_file=dataset_root + 'nuscenes_infos_train_sweep.pkl',
        # 👇 加这一行，只用前100个样本
        indices=list(range(100))
    ),
)
```

### 技巧3：消融实验

**想知道RHGM和RadarBEVNet各自的作用？**

```bash
# 实验1：只用RHGM
# 配置文件里：rhgm_module enabled=True, 用回原来的radar_voxel_encoder

# 实验2：只用RadarBEVNet
# 配置文件里：rhgm_module enabled=False, 用RadarBEVNet

# 实验3：都用（默认）
# 比较三个实验的mAP/NDS
```

---

## 📞 需要帮助？

如果遇到解决不了的问题：

1. **检查日志**：`work_dirs/racformer_with_rhgm_radarbevnet/` 里的日志文件
2. **查看代码注释**：代码里有详细的中文注释
3. **参考对比报告**：`模块代码对比报告.md` 里有详细的模块说明

---

## 🎉 恭喜完成！

如果你走到这一步，说明你已经掌握了：

- ✅ 如何安装和配置RaCFormer
- ✅ 如何训练自己的模型
- ✅ 如何测试和评估
- ✅ 如何解决常见问题

现在你可以：

- 尝试在自己的数据集上训练
- 调整参数优化性能
- 修改代码实现新想法

**加油！🚀**

---

*最后更新：2025-12-23*
*作者：AI Assistant*
*难度：⭐⭐ (适合新手)*
